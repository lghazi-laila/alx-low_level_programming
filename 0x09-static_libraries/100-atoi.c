#include "main.h"

/**
  * _atoi - a function that copies the string pointed to by src,
  * including the terminating null byte (\0),
  * to the buffer pointed to by dest.
  *
  * @s: A pointer to destination of string
  * Return: 0
  */
int _atoi(char *s)
{
	return (0);
}
