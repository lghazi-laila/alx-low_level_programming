#include "main.h"

/**
 * add - Description: Function that adds two integers and returns the result
 * @a: input integer
 * @b: input integer
 * Return: Result to add a and b
 */
int add(int a, int b)
{
		return (a + b);
}
